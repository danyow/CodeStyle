namespace CodeStyle.LineBreaksAndWrapping
{
    public class Enumerations6
    {
        // Keep existing arrangement of enumeration
        public enum Enum100
        {
            a,
            b,
            c
        }

        public enum Enum101
        {
            a,
            b,
            c
        }

        public enum Enum102
        {
            a,
            b,
            c,
            d
        }
        //     Max enumeration members on a single line
        public enum Enum1
        {
            val1
        }

        public enum Enum2
        {
            val1, val2
        }

        public enum Enum3
        {
            val1, val2, val3
        }

        public enum Enum4
        {
            val1, val2, val3,
            val4
        }

        public enum Enum5
        {
            val1, val2, val3,
            val4, val5
        }

        public enum Enum6
        {
            val1, val2, val3,
            val4, val5, val6
        }

        public enum Enum7
        {
            val1, val2, val3,
            val4, val5, val6,
            val7
        }

        public enum Enum8
        {
            val1, val2, val3,
            val4, val5, val6,
            val7, val8
        }

        public enum Enum9
        {
            val1, val2, val3,
            val4, val5, val6,
            val7, val8, val9
        }
        // Place simple enumeration on single line
        public enum Enum10
        {
            a, b, c
        }
        // Wrap enumeration declaration
        
        public enum Enum11
        {
            a,
            b,
            c
        }

        public enum Enum12
        {
            aaaaa,
            bbbbb,
            ccccc,
            ddddd,
            eeeee,
            fffff
        }
    }
}